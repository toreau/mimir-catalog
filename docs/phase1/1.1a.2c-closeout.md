# Phase 1.1A.2c — Closeout evidence (corrected)

Status: corrected corpus validation executed against the published Pass-B corpus.
Verdict: **GO — define workload and metrics contract** (subject to senior review).
No full Wikidata source scan was performed. No Pass-A/Pass-B artifact was modified.

## Correction history

- The first Phase-1.1A.2c validation run was **superseded** due to validator
  closeout defects: (1) edge target-order/duplicate checks could miss a violation
  exactly across a Parquet row-group boundary; (2) the lexical ordering gate used
  the previous `Value` string as an implicit "has previous key" sentinel, skipping
  the check when the previous raw value was the empty string; (3) evidence and
  publication semantics were incomplete. The corpus itself was not invalidated by
  these defects.
- Corrected run id: `20260903062845594` (evidence under `pass-c/`). The earlier
  corrected run `20260903062823752` was replaced by this run purely to capture the
  authoritative external RSS under the wrapper methodology.

## Input artifact identities (recorded in validation.json `inputs`)

- T2 artifact: count 4,480,182 / bytes 35,841,456 / SHA-256 `bd5e01cb38cfd6a2651fa9dac3d8ca7d09da3bd0342f2333f64e5a342c1031f8`
- concept.parquet 33,146,660 B `b41369f8…`; lexical_entry.parquet 217,125,949 B `20bfc66a…`;
  instance_of.parquet 19,185,779 B `2abe4890…`; subclass_of.parquet 28,965,324 B `29f2e6d1…`
  (full SHA-256 in `validation.json`); `pass-b.state.json` exact `Complete`.
- Phase-0 fixture `validation/phase0-anchors-v1.json` SHA-256
  `403deb7524f66f48d217b6bb1cdd70c9b46514c6386539a8bdb7993c2e38c59e`; schema and
  provenance (phase0 commit `b65f0f4`, source paths/hashes) validated by the loader.

## Exact integrity results (all passed, zero failed gates)

- Concept rows == unique == 7,403,488; no (false,false) flags; every `InT1=true`
  satisfies the T1 hash predicate.
- Tiers: T1 3,036,124; T2 4,480,182; T1∩T2 112,818; T2-only 4,367,364.
- Tail: 20 rows, all `InT1=false, InT2=true`, strictly ascending, no lexical rows.
  `tail_hash_qualified = 0` (no tail QID hashes into the T1 bucket); no
  hash-qualified `InT1=false` Concept outside the tail.
- Concept InT2 set == T2 artifact exactly (4,480,182).
- LexicalEntry 7,121,880 rows; decomposition exact (T1 en 2,321,048 / nb 108,605,
  alias en 370,246 / nb 14,478; T2-only en 4,217,221 / nb 90,282); policy,
  duplicate and within-QID ordering checks (including empty raw values) passed.
- InstanceOf 3,202,468 rows; every subject InT1; ordinal-monotonic; per-subject
  targets strictly ascending **with state persisted across Parquet row-group
  boundaries** (no boundary hole).
- SubclassOf 5,233,394 rows; every endpoint in T2; ordinal-monotonic; per-subject
  targets strictly ascending across row-group boundaries; P279 structural
  reconciliation via T2-index bitsets exact: subjects 4,467,638, objects 297,089,
  endpoint union == T2.

## Statistical diagnostics (not gates)

- T1 item fraction 2.500114 % (+138; reference binomial z ≈ 0.08, diagnostic only).
- Global T1 shares: P31 2.500296 %, en labels 2.501162 %, nb labels 2.498239 %,
  en aliases 2.499909 %, nb aliases 2.501278 %.
- T1 outgoing P31 degree: median 1, p90 1, p95 2, p99 2, max 18, mean 1.055.
- T1-subject outgoing P279 degree: median 0, p90 0, p95 0, p99 1, max 14
  (SubclassOf complete, not sampled).
- P31 target fanout: 27,182 sampled distinct targets, max fanout 1,146,555
  (≈ 45.9M × 0.025); 200 top Pass-A probe targets with sampled/expected ratios.

## Phase-0 anchor continuity (fixture; continuity evidence, not a corpus gate)

QID coverage in corpus: resolvedGold 70/132, outsideCoverage 44/63,
goldUnion 120/209, discoveryEvaluationTargets 114/200, ambiguousCand 64/108,
acquisitionSeedsAll 15/24. Lexical-surface continuity (resolved cases): 45 label
surface, 0 alias surface, 27 present-but-surface-absent, 53 concept absent, 47 not
applicable to the baseline lexical corpus.

## Diagnostic observations (neutral; not warnings)

- `tail_hash_qualified_count = 0`: the real 20-row tail did not exercise the
  hash-qualified exception (the rule is proven separately by the Q164 synthetic
  regression test).
- `resolved WikidataAlias raw-surface hits = 0`: a measured continuity result.
  T2-only concepts are intentionally label-only; this does not claim a
  contract-guaranteed property beyond that design rule.

No actual warnings were produced (`warnings: []`).

## Rationale for GO

All corrected exact integrity/materialization gates passed, including cross-row-group
edge ordering; T1 behaves as the intended deterministic 2.5 % subject sample;
complete P279 structure is preserved exactly; anchor continuity is recorded for
Phase 1.1A.3 without constraining the open-world corpus. No source scan; no
Pass-A/Pass-B artifact change; nothing committed or pushed.

## Operational

- Wall: ~6.3 s (in-process Stopwatch).
- Authoritative external peak RSS (`/usr/bin/time -l`): 767,066,112 bytes.
